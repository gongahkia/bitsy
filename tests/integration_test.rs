// Integration tests for Bitsy editor

// Note: Editor::new() requires terminal access, so we test components individually

// Integration tests will be added as features are implemented
// For now, component tests are in unit tests within each module
